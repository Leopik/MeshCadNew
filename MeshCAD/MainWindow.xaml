<Window x:Class="MeshCAD.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:HelixToolkit="clr-namespace:HelixToolkit.Wpf;assembly=HelixToolkit.Wpf"
        xmlns:System="clr-namespace:System;assembly=mscorlib"
        xmlns:diag="clr-namespace:System.Diagnostics;assembly=WindowsBase"
        xmlns:local="clr-namespace:MeshCAD.UIModels"
        xmlns:trlocal="clr-namespace:MeshCAD"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <ObjectDataProvider x:Key="dataFromEnum" MethodName="GetValues"
                        ObjectType="{x:Type System:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="trlocal:ClickMode"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <!--<StackPanel Height="401" VerticalAlignment="Top" Grid.Column="1" >-->
        <DockPanel LastChildFill="True" Grid.Column="1">
            <Button DockPanel.Dock="Top" Content="Открыть файл" Click="Button_Click" Height="24"/>
            <ComboBox DockPanel.Dock="Top"
                      ItemsSource="{Binding Source={StaticResource dataFromEnum}}"
                  SelectedItem="{Binding CurrentSelectMode}" />
            <Label DockPanel.Dock="Top" Content="Информация:" VerticalAlignment="Top" Height="26"/>
            <WrapPanel DockPanel.Dock="Top"  Orientation="Horizontal">
                <CheckBox Content="Показывать" IsChecked="{Binding CurrentChosenElement.IsShown}"/>
                <Button Content="Подсветить" Width="75" Click="HighlightElementClick"/>
            </WrapPanel>
            <TextBlock DockPanel.Dock="Top" x:Name="InfoBlock" TextWrapping="Wrap" Height="149" Text="{Binding CurrentChosenElement.ModelElement, UpdateSourceTrigger=PropertyChanged}"/>
            <Label DockPanel.Dock="Top" Content="Поиск элемента" Height="25"/>
            <WrapPanel DockPanel.Dock="Top"  Orientation="Horizontal">
                <ComboBox />
                <TextBox x:Name="SearchNumberTextBox" Text="TextBox" PreviewTextInput="SearchNumberTextBox_PreviewTextInput" DataObject.Pasting="SearchNumberTextBox_Pasting"/>
                <Button Content="Поиск" Width="75"/>
            </WrapPanel>
            <Label DockPanel.Dock="Top" Content="Структура" Height="25"/>
            <TreeView DockPanel.Dock="Bottom" x:Name="StructureTree">

                <TreeView.Resources>
                    <HierarchicalDataTemplate DataType="{x:Type trlocal:GroupTreeViewItem}">
                        <StackPanel Orientation="Horizontal">
                            <CheckBox  />
                            <Label Content="{Binding Title}"/>
                        </StackPanel> 
                    </HierarchicalDataTemplate>
                    <DataTemplate DataType="{x:Type local:BaseUIElement}">
                        <StackPanel Orientation="Horizontal">
                            <CheckBox IsChecked="{Binding IsShown}" />
                            <Button Content="{Binding Title}" Click="StructureElementClick"/>
                        </StackPanel>
                    </DataTemplate>
                </TreeView.Resources>
                <!--<TreeView.Resources>
                    <DataTemplate DataType="{x:Type local:BaseUIElement}">
                        <StackPanel Orientation="Horizontal">
                            <CheckBox IsChecked="{Binding IsShown}" />
                            <Button Content="{Binding Title}" Click="StructureElementClick"/>
                        </StackPanel>
                    </DataTemplate>
                </TreeView.Resources>-->
                <!--<TreeView.ItemTemplate>
                    <HierarchicalDataTemplate ItemsSource="{Binding Path=Phones}">
                        <StackPanel>
                        <CheckBox />
                        <TextBlock Text="{Binding Name}" />
                        </StackPanel>
                        <HierarchicalDataTemplate.ItemTemplate>
                            <DataTemplate DataType="{x:Type local:BaseUIElement}">
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox IsChecked="{Binding IsShown}" />
                                    <Button Content="{Binding Title}" Click="StructureElementClick"/>
                                </StackPanel>
                            </DataTemplate>
                        </HierarchicalDataTemplate.ItemTemplate>
                    </HierarchicalDataTemplate>
                </TreeView.ItemTemplate>-->
                <trlocal:GroupTreeViewItem Title="treet"/>
                <TreeViewItem x:Name="RectangleTree"  Header="Прямоугольники" />
                <TreeViewItem x:Name="VertexTree" Header="Узлы" />
                <TreeViewItem x:Name="TriangleTree" Header="Треугольники" />
                <TreeViewItem x:Name="RodTree" Header="Стержни" />
            </TreeView>
        </DockPanel>
        <!--</StackPanel>-->
        <HelixToolkit:HelixViewport3D 
            Grid.Column="0" 
            x:Name="ViewPort" 
            ZoomExtentsWhenLoaded="True" 
            Background="LightGray" 
            IsHeadLightEnabled="True" 
            ZoomAroundMouseDownPoint="True" 
            RotateAroundMouseDownPoint="True">

            <!--<HelixToolkit:BoundingBoxVisual3D BoundingBox="{Binding SelectedObject, Converter={StaticResource BoundsConverter}}" Diameter="0.2"/>-->
        </HelixToolkit:HelixViewport3D>

    </Grid>

</Window>
